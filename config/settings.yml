# config/settings.yml for rails-settings-cached
defaults: &defaults
  homepage_hardcoded: false
  homepage_image_url: ''

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
